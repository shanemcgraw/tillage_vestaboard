{
  "name": "beacon",
  "version": "1.0.0",
  "description": "Email-to-Vestaboard gateway with admin review",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "hash-password": "node -e \"const bcrypt = require('bcrypt'); const pw = process.argv[1]; bcrypt.hash(pw, 10).then(h => console.log(h));\"",
    "init-db": "psql $DATABASE_URL -f scripts/init-db.sql"
  },
  "engines": {
    "node": ">=20.x"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "@sendgrid/mail": "^8.1.0",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "multer": "^2.0.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "prisma": "^7.2.0"
  }
}
